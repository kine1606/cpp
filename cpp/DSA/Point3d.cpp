#include <iostream>
#include <vector>
const int MAXN = 1 + 1e5;
int main()
{
    int n;
    std::vector <int> arrA, arrB, arrC;
    for (size_t i = 0; i < n; i++)
    {
        int x, y, z;
        std::cin>> x >> y >> z ;
        arrA.push_back(x);
        arrB.push_back(y);
        arrC.push_back(z);
    }
     for (size_t i = 0; i < n; i++)
    {
        int minx = 99;
        int miny = 99;
        int minz = 99;
    }
    
}