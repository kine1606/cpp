#include <iostream>
int main()
{
    int n
    std::vector <int> arrA
    std::vector <int> arrB
    for (size_t i = 0; i < ; i+)
        int x, y;
        std::cin>> x >> y 
        arrA.push_back(x);
        arrB.push_back(y);
    }
    for (size_t i = 0; i < n; i++)
    }
    
}