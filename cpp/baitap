class Solution {
public:
    int romanToInt(string s) {
        int count(0);
        for(int i =s.size() -1 ; i > =0; i--)
        {
            if(s[i] == 'I') count++;
            else if(s[i] == 'V') count+=5;
            else if()
        }
    }
};