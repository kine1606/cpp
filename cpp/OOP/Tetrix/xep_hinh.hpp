#pragma once
#include <vector>
#include <iostream>
#include <time.h>

std::vector<std::vector<bool>> list(8, std::vector<bool>(10, false));

int toa_do_x, toa_do_y;
void Init_list();
void Xuat_List();
